<%= form_for(@task) do |f| %>
    <% if @task.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>
        <ul>
        <% task.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="task_form">

        <%= f.fields_for :tasks do |t| %>
        <%= t.label :Task %><br>
        <%= t.text_field :name %><br>
        <%= t.label :Content %><br>
        <%= t.text_field :content %><br>
        <%= t.label :Deadline %><br>
        <%= t.text_field :deadline %><br>
        <%= t.label :Status %><br>
        <%= t.text_field :status %><br>
        <% end %>
</div>

          <div class="actions">
            <%= f.submit value:"Register"%>
          </div>
    <%= link_to "Retour ", tasks_path %>
  <% end %>
