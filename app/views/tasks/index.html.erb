<h1>Task list</h1>
<table>
  <tr>
    <th>Task name</th>
    <th>Content</th>
    <th>Deadline</th>
    <th>Status</th>
    <th>OrderTask</th>

  </tr>
<% @tasks.each do |task| %>

  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
    <td><%= task.deadline %></td>
    <td><%= task.status %></td>
    <td><%= task.orderTask %></td>

      <td><%= link_to 'Check the details', task_path(task.id) %></td>
      <td><%= link_to "Edit task", edit_task_path(task.id) %></td>

   <td><%= link_to 'supprimer', task_path(task.id), method: :delete ,data: { confirm: 'voulez vous supprimer？' } %></td>

  </tr>
<% end %>

</table>
<br>
<%= link_to 'creer a new task', new_task_path %>
